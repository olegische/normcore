# Tests Agent Instructions

Run tests from the worktree root:

```
uv run --python .venv/bin/python --extra test -m pytest
```

Notes:
- The project requires Python 3.10+.
- Use the `test` extra to install pytest dependencies.
